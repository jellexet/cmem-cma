name: cmem_cma_CI

on:
  push:
  pull_request:

jobs:
  code_style:
    name: Code style
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            clang-format \
            linux-headers-$(uname -r) \
            libnuma-dev

      - name: Check formatting
        run: |
          files=$(git ls-files '*.c' '*.h' '*.cpp' '*.hpp')

          if [ -z "$files" ]; then
            echo "No C source/header files found."
            exit 0
          fi

          clang-format --dry-run --Werror $files

  build:
    name: Build kernel module
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install kernel build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            linux-headers-$(uname -r) \
            libnuma-dev

      - name: Build kernel module
        run: |
          make
